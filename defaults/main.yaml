---
cgit_css: ""

# the hostname used in nginx config's server section
cgit_hostname: ""
cgit_scan_path: /home/git
cgit_packages:
  - cgit
  - nginx
  - fcgiwrap

cgit_acl: |
    deny  192.168.0.1;
    allow 192.168.0.0/24;
    allow 10.0.0.0/24;
    deny  all;
